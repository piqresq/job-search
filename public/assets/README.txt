Place PNG assets here (same filenames the dashboard expects):

  linkedin.png
  google.png
  other.png
  wand.png
  link.png

Logos are shown at 28x28 CSS box (object-fit: contain).

Job row country / employment: location.png and time.png (see .row-ico in dashboard.html).
