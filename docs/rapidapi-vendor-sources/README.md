# Optional verbatim vendor docs

If RapidAPI’s live docs change often and you want a **full copy-paste archive** (e.g. from the playground or PDF), put it here:

- Suggested filenames: `fantastic-jobs-linkedin-api.txt`, `jsearch-openweb-ninja.txt`

**Do not** put API keys or `.dev.vars` contents in these files.

Agents and humans should prefer the curated summary in **`../rapidapi-job-providers.md`** for day-to-day work; use this folder only when you need the exact wording from a snapshot.
